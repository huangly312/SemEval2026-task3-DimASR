# Repo修改记录
用于hly和hjw协作，若有人修改当前代码结构，要在此文档中记录修改点，以便于对方查看阅读当前项目结构有哪些变化
## 当前项目结构
```
SemEval2026-task3-DimABSA
    /data # 数据集
        /raw_track_a # 原始数据集
    /script # 脚本代码文件
    /subtask_1 # subtask_1相关内容（模型、config、数据处理、评估、训练、输出结果）
    ├── config.yaml              # 配置文件(数据集、模型、超参数)
    ├── main.py                  # 主训练脚本
    ├── model.py                 # TransformerVARegressor模型定义
    ├── data_processing.py       # 数据加载和预处理
    ├── example_data_processing.py  # 单个样本经过 jsonl_to_df 和 VADataset 的处理过程
    ├── evaluation.py            # 评估指标计算
    ├── checkpoints/             # 最优模型保存目录
    ├── logs/                    # 训练日志保存目录
    ├── output/                  # 预测结果输出目录
    └── README.md                # subtask_1的说明文档
    README.md 
    修改记录.md
```
#### 20251121@hly
1. 新建项目、创建初始文件夹结构
2. 创建.gitignore文件（阻止无关文件被加入到 Git；保持仓库简洁、专业）
3. 上传track_a的raw数据


#### 20251125@hjw
1. 创建subtask_1文件夹
2. 基于start kit重构整个文件,目前可以通过.yaml文件配置参数，运行各baseline,只需要在主目录下运行`python subtask_1/main.py`即可

#### 20251126@hly
1. 增加绘制VA分布直方图的py文件 va_distribution_analysis.py，位于script文件夹
2. 运行方式 python ./script/va_distribution_analysis.py
3. 输出结果位于 ./script/script_output 文件夹
4. 为便于直接查看运行结果，运行过程的打印输出已经保存在 ./script/script_output/va_analysis_summary_output_log.txt

#### 20251127@hly
1. 修改 va_distribution_analysis.py 函数，修改无法识别finance领域的数据的逻辑
2. 20251126的运行结果保存在 ./script/script_output/1126 文件夹中

#### 20251128@hjw
1. 新增绘制token长度分布直方图的py文件 token_length_statistic.py，位于script文件夹
2. 运行方式 python ./script/token_length_statistic.py
3. 输出结果位于 ./script/token_length_output 文件夹

#### 20251130@hly
1. 修改va_distribution_analysis.py 函数，统一坐标取值范围和字体显示等问题
2. 调整目录结构，删除无关文件夹

#### 20251203@hly
1. 在subtask1中增加 example_data_processing.py ，展示单个样本经过 jsonl_to_df 和 VADataset 的处理过程
2. 在 /subtask_1/main.py 函数中修改关于模型加载时传入model_path的逻辑
3. 修改 /subtask_1/data_processing.py 函数，过滤掉 df['Aspect'] != 'NULL' 的字段

#### 20251204@hjw
1. 更新了subtask_2&3文件夹，增加了pipeline_based_method和llm_based_method文件夹,分别上传了pipeline的脚本以及基于unsloth的llm脚本(trl的待优化)
2. 上述的脚本均支持排除NULL字段
3. 其他运行的细节在各个文件夹里的readme中,根据部分结构原因,pipeline的脚本需要将数据集迁移到data文件夹下并在该文件夹下才能运行. llm的在根目录即可.